[project]
name = "exercise-notes-server"
version = "0.1.0"
description = "Starter MCP server for the Build phase exercise: add tools and wire the note resource."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
]
